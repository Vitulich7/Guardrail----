# Библиотека для загрузки и работы с предобученными трансформерами (DialoGPT, GPT-2)
transformers==4.34.0

# PyTorch — основная библиотека для работы с нейросетями (нужна для transformers)
torch>=1.13.0

# Streamlit — для создания веб-интерфейса и демонстрации чат-бота
streamlit==1.25.0

# NLTK — библиотека для обработки естественного языка, нужна для метрик (BLEU, ROUGE)
nltk==3.8.1

# Matplotlib — библиотека для визуализации результатов и метрик
matplotlib==3.7.1

# FAISS — для поиска по векторным индексам (нужно установить отдельно, если используешь)
faiss-cpu

# Дополнительные полезные библиотеки (если используешь)
scikit-learn
